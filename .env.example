# GoHighLevel — used ONLY by the dev MCP server (.mcp.json), NOT by the app.
# The app reads its GHL credentials per-client from DASHBOARD_CLIENTS below.
GHL_API_TOKEN=
GHL_LOCATION_ID=

# Anthropic (server-side only, AI assistant)
ANTHROPIC_API_KEY=

# Dashboard access gate (server-side only, never exposed to the browser).
#
# DASHBOARD_ACCESS_PASSWORD: ONE shared password for the whole internal team.
#   It gates the deployment; past the gate any user may open any project.
#   NOTE: in a .env file a value containing `$` should be single-quoted, or
#   dotenv may try to expand it. In the Vercel UI paste the raw value with no
#   quotes — quotes there become part of the password.
#
# DASHBOARD_CLIENTS: JSON array — one entry per project we commercialize. Each
#   entry carries its own GHL token + location, selected via the project picker.
#   Use `pnpm add-client` to extend this safely.
#
#   [{"id":"yconia","name":"Yconia","locationId":"abc123","ghlToken":"pit-..."}]
#
# DASHBOARD_AUTH_SECRET: random string signing both cookies (openssl rand -hex 32).
#   Rotating it invalidates every live session.
#
# DOMUS_ACCESS_PASSWORD / IW_ACCESS_PASSWORD: the narrower scopes. A session
#   opened with one of these can only reach the projects that scope names in
#   lib/scopes.ts (Domus: Condesa, Yconia, Plaza Bosques — IW: Condesa). Leave a
#   scope's password unset and that scope simply does not exist in the
#   deployment. Same `$`/quoting caveat as above.
DASHBOARD_ACCESS_PASSWORD=
DOMUS_ACCESS_PASSWORD=
IW_ACCESS_PASSWORD=
DASHBOARD_CLIENTS=
DASHBOARD_AUTH_SECRET=
